commit_message=$(git log -1 --pretty=format:"%s")

commit_hash=$(git rev-parse --short HEAD)

handleWriteLog() {
    echo "$commit_hash $commit_message" >>./version
    return 1
}

if handleWriteLog; then
    echo ""
else
    echo "命令执行失败，进行错误处理"
fi
